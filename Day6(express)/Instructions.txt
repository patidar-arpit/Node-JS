1. npm init -> to create the package.json file, which contains all the information related to the
                express versions
2. npm install -> to install all the dependency
(npm install body-parser nodemon)

3. npm install express -> to install express

4. No need to send the node-module package everytime, just include the package.json file and do
    npm install, it will automatically do the work

